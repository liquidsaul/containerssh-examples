apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "containerssh.fullname" . }}-test-connection"
  labels:
    {{- include "containerssh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "containerssh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
